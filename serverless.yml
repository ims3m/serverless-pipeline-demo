service: serviceDemo

provider:
  name: aws
  runtime: python3.11
  memorySize: 256
  region: ${file(./env_config/env_dev.yml):region}
  stage: ${file(./env_config/env_dev.yml):stage}

functions:
  serviceDemo:
    handler: src/app.lambda_handler
    events:
    - http:
        path: /service/demo
        method: get
